{
  "name": "backend",
  "scripts": {
    "start": "vercel dev",
    "deploy": "vercel",
    "build": "bun build api/index.ts --outdir dist --target node"
  },
  "dependencies": {
    "hono": "^4.7.7"
  },
  "devDependencies": {
    "vercel": "^32.4.1"
  }
}